# Questo file è solo un indice. Le variabili vere e complete
# per il backend sono in backend/.env.example (arriverà nella FASE 2).
#
# REGOLA D'ORO: questo file (.env.example) si committa su Git.
# Il file .env (senza "example") NON si committa mai.
